# Bug hunt — 2026-04-18 (Caldera exercise against test001)

Bugs found during live exercise, tracked here for PR #146 or follow-up work.
Severity: **blocker** = prevents core workflow, **major** = feature looks broken,
**polish** = cosmetic.

## Already fixed in PR #146

1. ✅ **blocker** SIGMA / graph_crypto_miner incidents shipped `entities=[]` — Service
   entity type was dropped during graph ingestion. Threats tab pivots empty.
2. ✅ **major** Date picker in Threats tab stuck on stale URL query (`?date=YYYY-MM-DD`
   from a previous session) — defaulted to yesterday on reload.
3. ✅ **blocker** Pivot tabs (IP / User / Detector) hidden inside `display:none`
   wrapper in HTML. UI only ever rendered default IP pivot.
4. ✅ **blocker** Detector-pivot drill-down returned empty journey
   (`PivotKind::Detector => None` short-circuit in investigation.rs).

## Discovered in this hunt

5. 🚨 **blocker** Retroactive dismiss races AI triage.
   `loops/slow_loop.rs:572-616` dismisses every Incident node whose
   `decision.is_none()`. Intended for pre-noise-gate historical incidents, but
   kills current incidents before AI finishes its 30s+ inference — 47 of 61
   decisions today are `dismiss` with reason *"Retroactive dismiss: historical
   incident with no decision"*. Net effect: responder never fires on real
   threats. **Fix candidate**: only dismiss incidents older than N minutes
   (e.g. ≥ 15 min) to let the AI triage loop finish first.

6. ℹ️ **polish** `GET /api/live-feed/geoip` returns HTTP 400 when called with
   no `ips=` param. Not called by the frontend; smoke-test artefact only.
   Optionally return empty list on empty query for robustness.

7. ℹ️ **not a bug** `GET /api/graph/threats` returns empty because
   `threat_intel_hits()` requires IPs tagged by threat-intel feeds. Caldera
   attacks run from local processes with no external IPs, so there are no
   hits — this is correct behaviour.

## To investigate

- `/api/graph/timeline` — 15 bytes (likely empty object, possibly legit).
- `/api/graph/process-tree` — 23 bytes.
- `/api/graph/path` — 11 bytes. Likely legit (no path between same source/dest).
- `/api/playbook-log` — 27 bytes. Playbooks may not have fired yet.
- `/api/correlation-chains` — 23 bytes.
- `/api/quickwins` — 28 bytes.
- `/api/advisory-cache` — 22 bytes.
- `/api/honeypot/sessions` — 15 bytes.

## Release-critical follow-up — AI budget architecture

Current design caps AI calls per tick (`max_ai_calls_per_tick=5`) which is
a **throughput** cap, not a **budget** cap. Under a coordinated attack of
N simultaneous Calderas (observed during this exercise: ~20-30 incidents
per minute), the cap just defers incidents into a growing backlog — so
a real attacker mixed into the noise can evade detection long enough to
get in.

The economics invert the intended safeguard: saving a few dollars of AI
spend to miss a compromise that costs thousands to clean up is the wrong
trade. The code already implements the right primitive (allowlist +
cooldown per `action:detector:entity`) to prevent runaway hammering on
the same incident; what is missing is a **budget-aware tiering** on top.

Desired architecture (spec-worthy, not yet implemented):

1. **Config**: replace `max_ai_calls_per_tick` with `ai_budget_usd_per_day`
   — default generous (e.g. $50) and tracked in the existing telemetry
   counter alongside `ai_provider_errors_per_hour`.
2. **Tiered AI fallback** — never drop an incident, degrade the classifier:
   - Tier 1 (default): cloud provider (OpenAI/Anthropic).
   - Tier 2: auto-fallback to local Ollama at 80% of daily budget.
   - Tier 3: heuristic rules-only if Ollama is down or saturated.
3. **Priority queue**: critical severity + first-seen entities always use
   Tier 1 regardless of budget state, so a volumetric attacker cannot
   starve the analysis of a targeted attacker riding the noise.
4. **Reverse circuit breaker**: if hourly AI spend exceeds 5× the 7-day
   baseline, pre-emptively drop to Tier 2 until the spike clears.
5. **Metrics**: `innerwarden_ai_tokens_total{provider,tier}` +
   `innerwarden_ai_usd_spent_total` for dashboard drift detection.

Justification — worst-case math on current code (50 concurrent attackers):
- Cooldown prevents re-calls on same `action:detector:entity`; practical
  ceiling ~250 unique combinations/hour = ~$15/h on GPT-4o, ~$0 on Ollama.
- But: a backlog means real attackers can slip through undetected, which
  is the expensive failure mode. Fix the backlog, not the budget.

## Spec 027 candidate — Multi-agent AI + MITRE-tiered auto-response

Emerged from the same Caldera exercise conversation. Two linked gaps that
the current single-provider, per-tick-capped AI pipeline cannot close:

### Why one model is not enough

qwen2.5:1.5b (default local) cannot reason about proportional response:
kill process vs isolate container vs leave honeypot vs do nothing requires
understanding the kill-chain context, operator environment tolerance, and
blast radius. Asking it to decide an active-defense playbook is asking the
wrong cognitive tier.

### Proposed multi-agent architecture

| Role | Model tier | Job | Budget per call |
|------|------------|-----|-----------------|
| Triage | qwen 1.5b local | "Noise or signal?" — drops 95% before deeper analysis | ~3s / $0 |
| Analyst | qwen 3b local or GPT-4o-mini | "Which MITRE technique? Corroborating evidence?" | 5-30s / $0.001 |
| Responder | Cloud (Sonnet/Opus) | "Given chain + capability + policy, which playbook?" | 30-120s / $0.05 |

Responder only fires for **decisive actions** (block/kill/isolate), which
in current data is <0.5% of incidents. Daily budget stays under $1 even
under sustained attack — the cheap local Triage does the filtering. The
existing `playbook.rs` engine stays the execution layer; the Responder
LLM picks and parameterises a playbook instead of picking a single skill.

### Why auto-block on Caldera-confirmed techniques is tempting but dangerous

Caldera exercises real MITRE techniques, but a technique fire ≠ an attack.
Real-world FP risk varies wildly by TID:

| MITRE TID | FP risk | Safe default |
|-----------|---------|--------------|
| T1486 (encrypt files / ransomware) | low | auto-block + quarantine |
| T1496 + YARA xmrig match | low | auto-kill process |
| T1055 (injection into RWX anon mapping) | low | auto-kill |
| T1014 (kernel module load) | medium | require chain corroboration |
| T1098 (account modify) | medium | notify + corroboration |
| T1547 (boot/autostart) | high | notify only |
| T1033 (`whoami`) | extreme | observe, feed graph |
| T1059 (shell exec) | extreme | observe |

A single TID rarely justifies a block. A **correlation chain** does —
`whoami → find /etc/shadow → outbound C2 → insmod` is an attack regardless
of any single step being benign in isolation. The existing 47 CL-*
correlation rules already encode that pattern, but there is no tier-map
that says "for T1486, block on sight" vs "for T1059, observe forever".

### What's missing / to be specified

1. `rules/mitre_auto_policy.toml` — per-TID policy tier (block / kill /
   isolate / notify / observe) + `require_corroboration: bool` + minimum
   chain length. Operator-editable.
2. Multi-tier AI routing in `ai/mod.rs`: triage / analyst / responder
   roles with fallbacks. Promote to cloud only when triage says "real"
   and analyst says "needs action decision". Local-first; cloud is
   opt-in per-tier.
3. Operator UI on Threats tab: per-TID override ("In my env, T1014 is
   never benign because I never load kernel modules manually — auto-block").
4. Budget tie-in: the $-per-day cap from the "AI budget" follow-up above
   flows into Responder tier admission (Triage and Analyst stay uncapped
   because they are local / cheap).

### Why it is spec 027, not today

The existing correlation engine + playbook engine are the right primitives;
this spec just layers policy tiers and agent routing on top. No sensor
changes, no schema migrations. Expected scope: ~2 weeks of work, gated on
benchmarking the Triage/Analyst/Responder split against recorded real
incidents (we have the data from this exercise already — the observation
verification logs include the AI decisions we would use as ground truth).

## Detection gaps from the Caldera 55 Metal exercise

Ground truth from today's live run on test001. Caldera invoked 28
distinct techniques (2232 total dispatches, 564 with `status=0`
success). InnerWarden produced 94 incidents concentrated in 5
detectors, mostly sigma (kernel module loads) and graph_crypto_miner.

The following MITRE technique categories **ran successfully on the host
and produced zero incidents**. Each needs a detector that matches the
behaviour plus a replay fixture in `testdata/` so regression is proven,
not claimed. Open an issue per category when test001 is reachable again.

| TID | Caldera ability | Ability UUID | Runs | Observed |
|-----|-----------------|--------------|------|----------|
| T1098.004 | Modify SSH Authorized Keys | `fc7f73fb2dc1ea3b3d85d14b7c8bb581` | 146 | 0 — this is the ability that locked the operator out of test001 |
| T1110.001 | SUDO Brute Force — Redhat | `3f4dd4cffe51d21011fbe98bd2c00c09` | 146 | 0 |
| T1110.004 | SSH Credential Stuffing From FreeBSD | `f9f460ac920bc836d0eec772f23493e2` | 90 | 0 |
| T1053.003 | Cron — Add script to all cron subfolders | `cf431ce197139329cceb7adfe08fe6fc` | 156 | 0 |
| T1053.002 | At — Schedule a job | `a451d8bc80be22b1ab53dc99663636b3` | 156 | 0 |
| T1048.001 | Exfiltration Over Alternative Protocol — SSH | `5ac7f6ec1898d8d408f9cdc687262cc7` | 156 | 0 |
| T1003.007 | Dump individual process memory with Python | `3b3fc24d410f4b700f3a1aba48b76532` | 209 | 0 |
| T1486 | Encrypt files using gpg (FreeBSD/Linux) | `add93724013a2cb88073c761dafa4a1c` | 75 | 0 (ransomware pattern) |
| T1485 | Overwrite file with DD | `a8fb0e278bbbc096194b88155368050e` | 85 | 0 (destructive) |
| T1222.002 | chattr — Remove immutable file attribute | `6030515771f31561f2b87d5832743888` | 85 | 0 |
| T1489 | Stop service by killing process using killall | `257fcac0f3e13b9c2ebbcee684c8b93c` | 75 | 0 |
| T1543.002 | Create SysV Service | `a9a7e30a21e6a7b5c28c9f0e41a293c0` | 20 | 0 |
| T1546.004 | System shell profile scripts | `0739c2e7c60bf7703b36a4f8f3480ccc` | 20 | 0 |
| T1140 | Base64 decoding with shell utilities | `ebd1464d50f4153155018c8afdd0e58b` | 85 | 0 |
| T1499 | Disrupt WIFI | `2fe2d5e6-7b06-4fc0-bf71-6966a1226731` | 36 | 0 |
| T1529 | Reboot System via `poweroff` | `e2a0dedb06b65b649f077c8e7e3f0d68` | 21 | 0 (if the ability actually rebooted, the tests would have failed — probably blocked by lack of root, but we did not detect the attempt either) |
| T1036.005 | Execute a process from a directory masquerading as the current one | `bef247bd0ac9b48f33f893fc937448cc` | 163 | 0 |
| T1021.004 | Start 54ndc47 (lateral sandcat spawn) | `10a9d979-e342-418a-a9b0-002c483e0fa6` | 146 | 0 — this is the ability that spawned 141 zombie agents |

### Detector categories that **did** work

- `sigma SIGMA-006` (Kernel Module Loaded) — fired on Diamorphine loads, 51 incidents. Solid.
- `graph_crypto_miner` — fired on xmrig, 19 incidents. Solid.
- `proto_anomaly` — caught SSH version anomalies from sandcat, 22 incidents. Noisy on operator traffic but correct.
- `integrity_alert` — caught the sed on `/etc/innerwarden/agent.toml` I did during the exercise. 1 incident.

### What the gap analysis does not say

- Dedup is not necessarily wrong: 75 xmrig runs → 19 incidents is mostly the `crypto_miner` detector cooldown collapsing repeats into one signal. Operator doesn't need 75 identical alerts. But the dashboard needs a "N attack attempts collapsed into 1 incident" counter so volume is visible.
- Responder never fired: the agent ran in `dry_run=true` throughout. In `guard_mode=true` the picture would include action attempts, but AI triage latency (~30s cold) means some incidents still slip the 15-minute retroactive-dismiss window — see the AI budget / multi-agent follow-ups above.

### What it does say

Most of the claimed MITRE coverage (55/55 in the navigator) is detectors
that exist in code but have **no proof they work on real traffic**. Four
detector categories stood up under a live exercise. The rest need
replay-fixture-backed regression tests before we can ship v0.11.x and
promise operators that we cover these techniques.

## Tooling notes

- Remote sudo on test001 requires password `dm2584` (operator kept `pkexec`
  gate, not NOPASSWD). Use `printf "%s\n" "dm2584" | sudo -S bash /tmp/script.sh`
  wrapped by SSH.
- Dashboard bind is `127.0.0.1:8787`. Mac-side port-forward: 8788 →
  `ssh -f -N -L 8788:127.0.0.1:8787 test001`.
