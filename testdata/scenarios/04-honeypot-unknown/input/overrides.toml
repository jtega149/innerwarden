# Spec 024 scenario #4 — honeypot hit from a clean IP.
#
# The scenario seeds a honeypot incident from 9.9.9.9 (no AbuseIPDB cache
# entry). Stub AI sees `ip_reputation = None` and returns Monitor instead
# of BlockIp, which is the "honeypot lets them in" contract. Composition
# test fails if anyone wires an auto-block path that ignores reputation.

[sensor.agent]
host_id = "scen-04-honeypot-unknown"
